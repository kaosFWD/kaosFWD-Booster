<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="kaosFWD Booster" Height="700" Width="1050" 
        Background="Transparent" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip" MinHeight="650" MinWidth="950"
        AllowsTransparency="True"
        WindowStyle="None">
    
    <Window.Resources>
        <!-- Gradients moderni -->
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6B73FF" Offset="0"/>
            <GradientStop Color="#9B59B6" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00D4AA" Offset="0"/>
            <GradientStop Color="#00A6FB" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="DangerGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6B6B" Offset="0"/>
            <GradientStop Color="#FF8E85" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="SurfaceGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1E1E1E" Offset="0"/>
            <GradientStop Color="#2D2D30" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- Stile per finestra con bordi arrotondati -->
        <Style x:Key="WindowBorder" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#0C0C0C" Offset="0"/>
                        <GradientStop Color="#1A1A1A" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#333333" Offset="0"/>
                        <GradientStop Color="#555555" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Stile pulsanti principali moderni -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="BackgroundBorder" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BackgroundBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.05" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="BackgroundBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.05" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BackgroundBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="BackgroundBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.0" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>
        
        <!-- Stile pulsanti secondari -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
        
        <!-- Stile per card/pannelli -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceGradient}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Stile titolo con effetto glow -->
        <Style x:Key="GlowTitle" TargetType="TextBlock">
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#6B73FF" BlurRadius="12" ShadowDepth="0" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Stile ListBox moderno -->
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Stile TextBox per log -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0F0F0F"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="12">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Finestra principale con effetti -->
    <Border Style="{StaticResource WindowBorder}" Margin="8">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Barra del titolo personalizzata con resize grip -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Area di trascinamento invisibile -->
                <Border Grid.Column="0" Grid.ColumnSpan="2" Background="Transparent" Height="40"/>
                
                <!-- Pulsanti finestra -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <!-- Pulsante minimizza -->
                    <Button x:Name="MinimizeButton" Width="30" Height="30"
                            Background="Transparent" BorderThickness="0"
                            Content="−" Foreground="#CCCCCC" FontSize="16"
                            Cursor="Hand" Margin="5,0"/>
                    
                    <!-- Pulsante chiusura -->
                    <Button x:Name="CloseButton" Width="30" Height="30"
                            Background="Transparent" BorderThickness="0"
                            Content="✕" Foreground="#CCCCCC" FontSize="14"
                            Cursor="Hand" Margin="5,0"/>
                </StackPanel>
            </Grid>

            <!-- HEADER MODERNO -->
            <Border Grid.Row="1" 
                    Style="{StaticResource ModernCard}"
                    Padding="25,20" 
                    Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="⚡ kaosFWD Booster" 
                               FontSize="32" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryGradient}"
                               HorizontalAlignment="Center"
                               Style="{StaticResource GlowTitle}"/>
                    <TextBlock Text="Ottimizzazione gaming di nuova generazione" 
                               FontSize="14" 
                               Foreground="#B0B0B0" 
                               HorizontalAlignment="Center" 
                               FontWeight="Light"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- PULSANTI PRINCIPALI CON DESIGN MODERNO -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,25" 
                        Grid.Row="2">
                <Button x:Name="BoostBtn" 
                        Content="🚀 BOOST GAMING" 
                        Width="180" 
                        Height="55" 
                        Background="{StaticResource AccentGradient}"
                        Style="{StaticResource ModernButton}"
                        Padding="15,12"
                        Margin="12"
                        FontSize="14"/>
                <Button x:Name="RestoreBtn" 
                        Content="🔄 RIPRISTINA TUTTO" 
                        Width="180" 
                        Height="55" 
                        Background="{StaticResource DangerGradient}"
                        Style="{StaticResource ModernButton}"
                        Padding="15,12"
                        Margin="12"
                        FontSize="14"/>
            </StackPanel>

            <!-- CONTENUTO PRINCIPALE CON LAYOUT MIGLIORATO -->
            <Grid Grid.Row="3" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- PANNELLO ESCLUSIONI RIDISEGNATO -->
                <Border Grid.Column="0" 
                        Style="{StaticResource ModernCard}"
                        Padding="20" 
                        Margin="0,0,15,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header sezione -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                    Background="{StaticResource AccentGradient}"
                                    CornerRadius="8"
                                    Padding="8"
                                    Margin="0,0,10,0">
                                <TextBlock Text="🛡️" FontSize="16" Foreground="White"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="PROCESSI PROTETTI" 
                                           FontSize="13" 
                                           FontWeight="Bold" 
                                           Foreground="White"/>
                                <TextBlock Text="Sempre sicuri durante il boost" 
                                           FontSize="10" 
                                           Foreground="#888888"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Lista esclusioni con stile moderno -->
                        <ListBox x:Name="ExclusionList" 
                                 Grid.Row="1"
                                 Style="{StaticResource ModernListBox}"
                                 Margin="0,0,0,15"/>
                        
                        <!-- Pulsanti con layout migliorato -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button x:Name="AddBtn" 
                                    Grid.Column="0"
                                    Content="➕ Aggiungi" 
                                    Background="#27AE60"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,6,0"/>
                            <Button x:Name="RemoveBtn" 
                                    Grid.Column="1"
                                    Content="🗑️ Rimuovi" 
                                    Background="#E74C3C"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="6,0,0,0"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- PANNELLO LOG RIDISEGNATO -->
                <Border Grid.Column="1" 
                        Style="{StaticResource ModernCard}"
                        Padding="20" 
                        Margin="15,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header log con indicatore live -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                    Background="{StaticResource PrimaryGradient}"
                                    CornerRadius="8"
                                    Padding="8"
                                    Margin="0,0,10,0">
                                <TextBlock Text="📊" FontSize="16" Foreground="White"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="LOG ATTIVITÀ" 
                                           FontSize="13" 
                                           FontWeight="Bold" 
                                           Foreground="White"/>
                                <TextBlock Text="Monitoraggio operazioni in tempo reale" 
                                           FontSize="10" 
                                           Foreground="#888888"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                            
                            <!-- Badge LIVE animato -->
                            <Border Grid.Column="2" 
                                    Background="#FF4757" 
                                    CornerRadius="12" 
                                    Padding="8,3">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="6" Height="6" 
                                             Fill="#FFFFFF" 
                                             Margin="0,0,4,0">
                                        <Ellipse.RenderTransform>
                                            <ScaleTransform x:Name="PulseTransform" ScaleX="1" ScaleY="1"/>
                                        </Ellipse.RenderTransform>
                                        <Ellipse.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PulseTransform"
                                                                                     Storyboard.TargetProperty="ScaleX">
                                                            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
                                                            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1.3"/>
                                                            <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
                                                        </DoubleAnimationUsingKeyFrames>
                                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PulseTransform"
                                                                                     Storyboard.TargetProperty="ScaleY">
                                                            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
                                                            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1.3"/>
                                                            <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
                                                        </DoubleAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Ellipse.Triggers>
                                    </Ellipse>
                                    <TextBlock Text="LIVE" 
                                               FontSize="9" 
                                               FontWeight="Bold" 
                                               Foreground="White"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        
                        <!-- Area log con bordo moderno -->
                        <Border Grid.Row="1"
                                Background="#0A0A0A" 
                                CornerRadius="12" 
                                BorderBrush="#333333" 
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.6"/>
                            </Border.Effect>
                            <TextBox x:Name="LogBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     IsReadOnly="True" 
                                     AcceptsReturn="True" 
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- FOOTER INFORMATIVO MODERNO -->
            <Border Grid.Row="4" 
                    Style="{StaticResource ModernCard}"
                    Padding="15,12" 
                    Margin="0,15,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0"
                            Background="#F39C12" 
                            CornerRadius="6"
                            Padding="6,3"
                            Margin="0,0,12,0">
                        <TextBlock Text="⚠️" FontSize="12" Foreground="White"/>
                    </Border>
                    
                    <TextBlock Grid.Column="1"
                               Text="Usa sempre 'RIPRISTINA TUTTO' per riaprire le applicazioni chiuse automaticamente" 
                               FontSize="12" 
                               Foreground="#D0D0D0" 
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                    
                    <TextBlock Grid.Column="2"
                               Text="v2.0 • kaosFWD"
                               FontSize="10"
                               Foreground="#666666"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>